<div class="jumbotron p-20">
  <h1 class="display-4 text-center">Добавь свой вариант ответа</h1>
  <p class="lead text-center">для нашего Magic 8 Ball</p>
  <hr class="my-4">
  <p>&nbsp;</p>
  <p class="lead">
    <%= form_for @answer do |f| %>


      <% if @answer.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@answer.errors.count, "error") %> запрещено сохранение этого выражения:
          </h2>
          <ul>
            <% @answer.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.text_field :title, class: "form-control form-control-lg" %>
        <small id="noticeHelper" class="form-text text-muted">Желательно, пиши по-русски, чтоб тебя все могли понять :)</small>
      </div>
      <div class="row justify-content-center">
        <%= f.submit "Запомнить фразу", class: "btn btn-success btn-lg" %>
      </div>

      <% if flash[:notice] %>
        <div class="alert alert-success alert-dismissible fade show mt-2" role="alert">
          <%= flash[:notice] %>
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="alert alert-danger alert-dismissible fade show mt-2" role="alert">
          <%= flash[:alert] %>
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
      <% end %>

    <% end %>
  </p>
</div>